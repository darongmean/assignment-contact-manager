# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           controllers.ContactListController.getIndex()
GET     /me                         controllers.ContactListController.getMe()

POST    /login                      controllers.ContactListController.postLogin()
POST    /contactList                controllers.ContactListController.postContactList(request: Request)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
